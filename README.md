# ai-automations
Basic AI 

To execute this 
    Run this command to install requirements
        python3 -m venv .venv 
        source .venv/bin/activate
        pip install -r requirements.txt
        python3 PrivateOverlayDemo.py
